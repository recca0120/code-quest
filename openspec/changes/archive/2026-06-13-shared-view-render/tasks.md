## Tasks

- [x] 建立 `renderPaneView` 函式（`pane-view-render.tsx`）
- [x] `tool-panes.tsx` 的三個 pane 改用 `renderPaneView`
- [x] `DrawerHost.tsx` 移除 `renderDrawerBody`，改用 `renderPaneView`
- [x] 確認所有既有測試通過 + tsc clean
