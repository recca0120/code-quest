## Tasks

- [x] SessionPane: 將 rail wrapper (`data-testid="chat-rail-wrapper"`) 從 TabContent prop 改為直接渲染在 TabContent 旁
- [x] TabContent: 移除 `rightPane` 和 `railWidth` props
- [x] ChatView: 移除 `rightPane` 和 `railWidth` props 及 rail wrapper div
- [x] 確認所有既有測試通過 + tsc clean
